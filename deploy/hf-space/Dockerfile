# Hugging Face Space - 精简 Dockerfile
# 完整镜像由 GitHub Actions 构建并推送到 GHCR，Space 仅拉取运行。
#
# 使用前请将 OWNER/REPO 替换为你的 GitHub 用户名与仓库名（全小写）。
FROM ghcr.io/wei-shaw/sub2api:latest

ENV PORT=7860
ENV SERVER_PORT=7860
ENV SERVER_HOST=0.0.0.0

EXPOSE 7860
